<% @comments.each do |comment| %>
  <p>
  <div>
    <small><time datetime="<%= comment.created_at_formated %>">
      <%= comment.created_at_formated %>
    </time></small>
  
  </p>

    <p>
      <%= comment.body %>
    </p>

    <p>
    
      <%= link_to 'Edit', edit_article_comment_path(@article, comment) %> |
      <%= link_to 'Destroy', article_comment_path(@article, comment), data: {method: :delete, confirm: 'Are you sure?'} %> 
    
    </p>
    </div>

    <p></p>

<% end %>
